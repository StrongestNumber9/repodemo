name: Trigger two

on: [ push ]
permissions: write-all
jobs:
  create_repo:
    runs-on: ubuntu-latest
    steps:
      - name: Create two
        uses: StrongestNumber9/dfg_01@initial_release
        with:
          from_repository: teragrep/pth_07
          from_version: 8.2.1
          to_repository: StrongestNumber9/pkg_01
          token: ${{ secrets.PAT_TOKEN }}
          max_attempts: 5
